#include<stdio.h>
#include<string.h>
int main() {
 char string[20];
 int i=0,len=0;
    scanf("%s",string);
    len=strlen(string);
    for(i=len-1;i>=0;i--){
        printf("%c",string[i]);
    }

    return 0;
}