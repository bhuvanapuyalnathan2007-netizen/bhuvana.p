#include<stdio.h>
long long factorial(int n) {
    long long factorial = 1;
    for(int i = 1; i <= n; i++) {
        factorial =factorial * i;
    }
    return factorial;
}
int main() {
    int n;
    scanf("%d",&n);
    printf("%lld",factorial(n));
    return 0;
}
