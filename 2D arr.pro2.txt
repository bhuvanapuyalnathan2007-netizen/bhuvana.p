#include<stdio.h>
int main() {
    int r,c;
    printf("Enter the row and column value:");
    scanf("%d %d",&r,&c);
    int arr[r][c];
    for(int i=0;i<r;i++) {
        for(int j=0;j<c;j++) {
            scanf("%d",&arr[i][j]);
        }
        int maximum=0;
        int max month=0;
    }for (int i=0;i<r;i++) {
        int sum=0;
        for (int j=0;j<c;j++) {
            sum=sum+arr[i][j];
        }
        if (sum<max sum){
            max sum = sum;
            max month=i;
            printf("%d has the highest sales",i+1);
        }
    }
    return 0;
}