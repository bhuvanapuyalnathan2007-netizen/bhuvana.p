#include<stdio.h>
#include<string.h>
int main() {
    char str[20];
    fgets(str,sizeof(str),stdin);
    int i=0;
    int count =0;
    while(str[i]!='\0') {
            if(str[i]=='A'&&str[i]=='Z'){
                count++;
        }

        i++;
        }
             if(count<=1){
                 printf("valid");
             }
    else{
        printf("invalid");

    }
         return 0;

    }

