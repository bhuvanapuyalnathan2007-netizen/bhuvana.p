#include<stdio.h>
int main()
{
    int size;
    scanf("%d",&size);
    int arr[size];
    for(int i=0;i<size;i++)
    {
        scanf("%d",&arr[i]);
    }
    int total=0;
    for(int j=0;j<size;j++)
        total+=arr[j];
    int max = arr[0];
    for (int k;k<size;k++)
    {
        int i;
        if (arr[i]>max)
        {
            max=arr[i];
        }
        printf("%d The total votes",total);
        printf("%d is the max  votes of",max);
    }
    return 0;
}